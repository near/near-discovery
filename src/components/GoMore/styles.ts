import styled from 'styled-components';

export const StyledMore = styled.div`
  display: flex;
  align-items: center;
  gap: 9px;
  color: #ebf479;
  text-align: right;
  font-family: Gantari;
  font-size: 16px;
  font-style: normal;
  font-weight: 400;
  line-height: normal;
  cursor: pointer;

  &:hover {
    opacity: 0.9;
  }
  &:active {
    opacity: 0.8;
  }
`;
