// eslint-disable-next-line @typescript-eslint/no-var-requires
const { generateHtml } = require("generate-html");

// TODO: add an array of paths to generate html for
generateHtml("dist", "https://beta.near.org");
